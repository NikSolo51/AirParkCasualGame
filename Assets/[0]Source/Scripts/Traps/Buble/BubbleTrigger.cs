using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BubbleTrigger : MonoBehaviour
{
    /*
    private void OnTriggerEnter(Collider other)
    {
        
        if (other.GetComponent<MovementPerson>() && !TowerOfPeople.towerOfPeopleBehavior.currentLinkIsPerson() && this.transform.childCount <= 0  )
        {
            Bubble.bubbleBehavior.peopleSetParentBubble(TowerOfPeople.towerOfPeopleBehavior.GetCurrentLink(),this.transform);
            Bubble.bubbleBehavior.peopleSetPositionBubble(TowerOfPeople.towerOfPeopleBehavior.GetCurrentLink());
            TowerOfPeople.towerOfPeopleBehavior.RemoveCurrentLink();
        }
    }
    */
}
